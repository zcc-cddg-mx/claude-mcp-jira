services:
  service:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs

  mcp:
    build:
      context: .
      dockerfile: jira_mcp/Dockerfile
    ports:
      - "8001:8001"
    env_file:
      - .env
    environment:
      - SERVICE_URL=http://service:8000
    volumes:
      - ./logs:/app/logs
    depends_on:
      - service

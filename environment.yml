name: claude-mcp-jira
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - anthropic
      - fastapi
      - "uvicorn[standard]"
      - httpx
      - requests
      - typer
      - pydantic
      - python-dotenv
      - "mcp[cli]"
      - starlette
